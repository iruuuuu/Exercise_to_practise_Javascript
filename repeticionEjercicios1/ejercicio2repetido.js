import { productos } from src/db/data;

function analizarProductosPorCategoria(productos){
    const categoriaProductos=((productos)=>{
        const categoria= productos.filter(productos=>productos.categoria(categoria))
        return categoria
    })

    const numeroTotalProductos=((productos)=>{
        const totalProductos= productos.map.filter(productos=>productos.reduce)
    })

        const precioMedioProductos=((productos)=>{
        const totalProductos= productos.map.filter(productos=>(precio.reduce)/productos.reduce)
    })
}



//no sabes hacerlo
